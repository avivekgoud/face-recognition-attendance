services:
  - type: web
    name: facesync-attendance
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    plan: free
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: BIOMETRIC_ENCRYPTION_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.9
